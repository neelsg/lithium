Li 0

pub read type:
	
	err error = error.ok
	
	.= func(fileName string):
		# Open a file for reading
		# TODO
	
	pub read func(n int.u64) (bytes array[int.u8], read int.u64, err error):
		# Read from the file
		# TODO
	
	pub seek func(offset int.s64, whence int) (err error):
		# Seek a position in the file
		# TODO
	
	pub close func(self readFile) error:
		# Close the file
		# TODO
	
	get error error:
		# Return the current error if any
		return err
